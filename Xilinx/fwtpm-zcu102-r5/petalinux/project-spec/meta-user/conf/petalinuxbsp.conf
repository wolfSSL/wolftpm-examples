#User Configuration

#OE_TERMINAL = "tmux"

# wolfTPM fwTPM additions:
#   fwtpm-r5         stages the R5 ELF to /lib/firmware/ for remoteproc.
#   fwtpm-rpmsg-test is the smoke-test client that runs on the A53 APU.
#   fwtpm-caps       is the decoded GetCapability hello-world client.
IMAGE_INSTALL:append = " fwtpm-r5 fwtpm-rpmsg-test fwtpm-caps"
